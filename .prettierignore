# Prettier is here to fill Biome's gaps — today that means Markdown and
# YAML, since Biome already owns every JS/TS/JSON file. Everything below is
# either somebody else's file or a format Prettier has no business
# reformatting.
node_modules/
.bun-cache/
bun.lock

# Written by the release job, not by hand.
CHANGELOG.md

# Vale's downloaded style packages.
styles/Google/
styles/proselint/
