# Require real status checks

## 1. Require the real checks

- [x] 1.1 Set `required_status_checks` (strict, real contexts) via the API
- [x] 1.2 Confirm `required_approving_review_count` is still `0`
